FROM nginx:alpine

# Copy sandbox nginx configuration
COPY nginx-sandbox.conf /etc/nginx/conf.d/default.conf

# Create iterations directory structure
RUN mkdir -p /usr/share/nginx/html/iterations

# Expose port 80
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
